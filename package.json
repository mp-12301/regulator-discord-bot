{
  "name": "regulator-discord-bot",
  "version": "1.0.0",
  "engines": {
    "node": "16.x"
  },
  "description": "shut up",
  "main": "index.js",
  "scripts": {
    "start": "ts-node ./src/index.ts",
    "dev": "nodemon --watch src --ext ts,json --exec 'node --inspect -r dotenv/config -r ts-node/register ./src/index.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@discordjs/opus": "^0.5.3",
    "@discordjs/voice": "^0.7.5",
    "discord.js": "^13.4.0",
    "ffmpeg-static": "^4.4.0",
    "ts-node": "^10.4.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.15"
  }
}
